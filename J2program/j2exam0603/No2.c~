#include<stdio.h>
#include<stdlib.h>
#include<time.h>

int main(void){
  int a;
  int b;
  int c;
  a=0;

  srand((unsigned)time(NULL));

  while(a<=617){
    b += rand()%32768;
    a++;
  }
  printf("wa = %d\n",b);
  
    c = b/617;
    printf("heikin = %d\n",c);

    return 0;
}
