#include <stdio.h>

#define MAX 10

void setNumber(int array[])
{

}

void dispNumber(int array[])
{
  
}

void doubleNumber(int array[])
{
  
}

int main(void)
{
  int array[MAX];

  setNumber(array);

  dispNumber(array);

  doubleNumber(array);

  dispNumber(array);
  
  return 0;
}
